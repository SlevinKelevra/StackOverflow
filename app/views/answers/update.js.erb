$('.errors').remove();
$('.notice').remove();
<% if @answer.errors.any? %>
$('.answers').append('<%= j render 'common/errors', resource: @answer %>');
<% else %>
$('.answers').html('<%= j render @answer %>')
<% end %>